# Plan

1. Extend user records with `openingCash` and `openingBank`.
2. Add `GET /api/profile` and `PUT /api/profile` for balance setup.
3. Add all-time balance summary alongside filtered monthly summary.
4. Update dashboard cards to show real cash in hand and bank account balance.
5. Add sidebar navigation links to major app sections.
6. Add a Balances panel for editing cash/bank starting balances.
7. Update API tests.
8. Run syntax checks and tests.
